node_modules
dist
dist-ssr
*.local
.env
.env.local
# Real e2e secrets (Foundry key, JWT_SECRET, QA password). Only the template
# .env.e2e.example is committed.
.env.e2e
.DS_Store

# Local-only dev DB (run locally with Docker; not committed)
docker-compose.yml

# Exported Cosmos logs (contain conversation content / PII; never committed)
exports/

# Playwright e2e — auth state holds JWTs; traces/videos capture JWTs + response
# bodies. Never commit; scrub before any future CI artifact upload.
playwright/.auth/
test-results/
playwright-report/
